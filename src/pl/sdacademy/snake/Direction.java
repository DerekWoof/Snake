package pl.sdacademy.snake;

/**
 * Enum reprezentujący kierunek (w przypadku naszej aplikacji będzie użyty do opisania kierunku ruchu węża).
 */
public enum Direction {
    RIGHT,
    LEFT,
    UP,
    DOWN
}
